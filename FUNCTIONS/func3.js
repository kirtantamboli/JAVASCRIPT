function welcomeMsg(name) {
    return ("Hello " + name + " welcome to myworld");
}
// creating a variable
let nameVal = "Admin";

// calling the function
console.log(welcomeMsg(nameVal));


//practice

function square(num){
    return (num * num) ;
}
let num = 5 ;

console.log(square(num));
